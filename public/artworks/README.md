Drop your artwork photos here using the exact filenames referenced in
`src/data/artworks.ts` (e.g. `krishna-flute-01.jpg`) and they will replace
the generated placeholders automatically — no code changes needed.
